# 日新教师 AI 工作台 · 发布记录

## V2.1 合并前修正版（2026-07-17）

- 全局搜索按工具与提示词的实际匹配数量进入对应资源库；两类均有结果时优先显示匹配更多的一类，无结果时留在首页并给出明确提示；
- 首页快捷任务收敛为与 5 条既有工作流一一对应的真实入口；
- 提高路径短标签、资源标签、核验信息、辅助说明和移动底部导航等界面文字的最小字号；
- 将“最近更新”改为“最近核验”，明确 `updatedAt` 表示目录内容确认或核对时间；
- 完成桌面端与移动端指定尺寸、搜索路由、Hash、清空搜索、弹窗、复制、隐私边界和横向溢出验收。

## V2.1 布局重构（2026-07-17）

### 本版定位

保留 V2 数据、搜索、任务、工作流、详情、复制和容错内核，把首页从纵向卡片目录重构为“轻侧栏导航 + 不对称 Bento 首页 + 独立工具库 + 独立提示词库”。

### 本版完成

- 桌面端使用 218px 固定轻侧栏，移动端使用顶部品牌栏和五项底部导航；
- 新增 `#home`、`#tools`、`#prompts` 三个视图，刷新后保留当前视图；
- 首页首屏集中呈现搜索、快捷任务、4 个常用工具和一条标准路径；
- 核心教学工具使用真实网页截图，专业专区采用无边框行式资源列表；
- 首页只保留 3 条精选提示词和 3 条有可靠日期的最近更新；
- 任务切换会更新推荐路径、核心工具、精选提示词和专区强调，不把首页重新变成长列表；
- 工具库保留两列紧凑列表、分类、在线筛选和内部项目折叠；
- 提示词库使用文档式列表，保留搜索、分类、复制和完整详情；
- 工作流步骤新增 `shortLabel`，首页用短动作、详情保留完整说明；
- 4 个内部/维护者项目的公开链接字段及 `pendingLinks` 已全部清空；
- 全仓搜索确认已知内部真实地址未出现在当前公开资产；
- 完成 7 组指定尺寸、Hash、搜索、筛选、工作流、复制、弹窗和移动任务抽屉验收。

### 数据概况

- 工具：15 条，其中公开在线 11 条，内部或维护者区域 4 条；
- 提示词：7 条；工作流：5 条；
- 3 个公开核心工具补充 `updatedAt: 2026-07-17`，表示目录内容本轮确认时间；
- 新增 `assets/tool-previews/curriculum-to-classroom-workbench.png` 真实工具截图。

### 发布提醒

- 当前版本只推送 `feat/v2-workbench-redesign`；
- 不自动合并 `main`，不自动部署 GitHub Pages；
- 合并前仍需维护者确认内部项目公开边界和错题记录直达链接。

---

## V2 预览版

## 本版定位

V2 从“展示有哪些工具”转向“帮助教师完成一个真实任务”，保留原生静态站点和全部既有工具、提示词、正式 URL、隐私说明与使用边界。

## 本版完成

- 首屏改为任务导向：全局搜索、轻量隐私提示和 5 个高频任务入口成为主要操作；
- 固定展示 6 个已上线核心工具，并与紧凑的全部工具列表建立视觉层级；
- 建立 6 个统一主分类、5 个任务分组和公开/内部/维护者可见性；
- 新增 `workflows.json`，用已有工具与提示词 ID 组织 5 条任务工作流；
- 搜索覆盖工具、提示词、别名、场景、标签和补充关键词，支持最多 6 条分组建议、Enter 定位和一键清空；
- 增加任务筛选、分类筛选、仅显示可直接使用、结果数量和清除筛选；
- 实验项目与维护者工具进入默认折叠区，前端不渲染内部 URL 或 `pendingLinks`；
- 工具与提示词采用不同信息形态，提示词支持复制、详情与失败后的手动复制；
- 重构原生 `<dialog>`，支持背景滚动锁定、Esc/遮罩关闭、焦点回归和详情切换滚动复位；
- 三个 JSON 独立加载和失败隔离，缺少 V2 字段时使用安全默认值；
- 新视觉系统使用浅灰背景、低饱和主色、统一线性 SVG 图标和克制阴影；
- 完成桌面、平板和手机响应式布局及移动菜单。

## 数据概况

- 工具：15 条，其中公开在线 11 条，内部或维护者区域 4 条；
- 提示词：7 条，其中首页默认推荐 6 条；
- 工作流：5 条；
- 推荐工具顺序：课标到课堂、写作思维小助手、四色证据链观察器、小学语文要素全景图、日新教师工具箱、班级作文报纸 / 在线杂志生成器。

## 验证记录

- 2026-07-17：JSON 解析、工作流引用、JavaScript 语法和 `git diff --check` 通过；
- 2026-07-17：10 个唯一公开 URL 均返回 HTTP 200；
- 2026-07-17：Chrome 冒烟覆盖搜索、任务工作流、分类筛选、详情弹窗、复制反馈、内部区折叠和移动菜单；
- 2026-07-17：360、390、768、1024、1280、1440 宽度均未出现页面级横向溢出。

## 仍待人工确认

- 活动长图排版器、这样想象真有趣、看见写作的公开入口继续保持内部状态；
- “这样想象真有趣”现有截图仍可能停留在妙搭加载页，需要真实可用截图后再替换；
- 错题记录模块仍通过日新教师工具箱总入口访问，直达链接待确认；
- 详细清单见 `pending-inputs.md`。

## 发布提醒

- 当前版本可作为 V2 预览版合并候选；
- 合并或部署前仍需维护者确认内部项目边界和截图状态；
- 本次交付不自动合并 `main`、不自动部署、不强制推送。
